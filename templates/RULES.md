# Standing rules

Curated directives distilled from decisions. Keep this short — every line here
is injected into agent context at session start. Promote a decision to a rule
only when it must be honored on *every* change, not just on guarded paths.

- (example) Never hand-edit generated artifacts; regenerate them.
