---
name: Feature request
about: Suggest an addition or change to zavet (commands, hooks, bin/zavet, dialect)
title: "[feat] "
labels: enhancement
---

**Problem**
What are you trying to do that zavet doesn't support today?

**Proposed solution**

**Alternatives considered**

**Anything else**
Note if this touches the frontmatter dialect (`.zavet/decisions/`, `.zavet/specs/`) or the
`dira zavet emit` event schema — those are shared contracts with `dodi-smart/dirahq-cli` and
usually need a matching change there too.
