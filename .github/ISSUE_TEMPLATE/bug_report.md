---
name: Bug report
about: Something in the zavet plugin (hooks, commands, bin/zavet) is broken
title: "[bug] "
labels: bug
---

**What happened**

**What you expected**

**Repro**
- Claude Code version:
- zavet version (`zavet version` or `/plugin list`):
- OS / shell:
- Steps to reproduce:

**Logs / output**
(hook stderr, `bin/zavet` command output, `sh test/run.sh` output, etc. — paste as text,
not a screenshot)

**Is this a security issue?**
If this could let a hook run something it shouldn't, or expose something it shouldn't, please
don't file it here — see [SECURITY.md](../../SECURITY.md) for private reporting instead.
